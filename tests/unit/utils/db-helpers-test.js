import dbHelpers from 'r-place-canvas-tool/utils/db-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | db helpers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dbHelpers();
  assert.ok(result);
});
